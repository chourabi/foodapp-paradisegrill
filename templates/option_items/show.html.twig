{% extends 'base.html.twig' %}

{% block title %}OptionItems{% endblock %}

{% block body %}
    <h1>OptionItems</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ option_item.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ option_item.Nom }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ option_item.isActive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('option_items_index') }}">back to list</a>

    <a href="{{ path('option_items_edit', {'id': option_item.id}) }}">edit</a>

    {{ include('option_items/_delete_form.html.twig') }}
{% endblock %}
