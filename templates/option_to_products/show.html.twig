{% extends 'base.html.twig' %}

{% block title %}OptionToProducts{% endblock %}

{% block body %}
    <h1>OptionToProducts</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ option_to_product.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('option_to_products_index') }}">back to list</a>

    <a href="{{ path('option_to_products_edit', {'id': option_to_product.id}) }}">edit</a>

    {{ include('option_to_products/_delete_form.html.twig') }}
{% endblock %}
