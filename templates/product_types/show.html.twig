{% extends 'base.html.twig' %}

{% block title %}ProductTypes{% endblock %}

{% block body %}
    <h1>ProductTypes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_type.id }}</td>
            </tr>
            <tr>
                <th>ProductType</th>
                <td>{{ product_type.productType }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('product_types_index') }}">back to list</a>

    <a href="{{ path('product_types_edit', {'id': product_type.id}) }}">edit</a>

    {{ include('product_types/_delete_form.html.twig') }}
{% endblock %}
